<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/inventoryrecyclerview_main"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="@color/BottomNav"
    app:cardElevation="0dp"
    app:cardCornerRadius="10dp"
    app:cardUseCompatPadding="true"
    app:strokeWidth="1dp"
    app:strokeColor="@color/CardStroke">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
        <TextView
            android:id="@+id/inventoryrecyclerview_nametextview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_margin="15dp"
            android:textSize="20sp"
            android:textColor="@color/textColor"
            android:textAlignment="center"
            android:drawablePadding="10dp"
            tools:text="Whiteout Pants x 2"
            tools:drawableTop="@tools:sample/avatars" />

        <com.google.android.material.chip.Chip
            android:id="@+id/inventoryrecyclerview_tradebanchip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_margin="5dp"
            android:clickable="false"
            android:textColor="@color/white"
            android:visibility="gone"
            app:chipBackgroundColor="@color/ChipColor"
            app:chipIcon="@drawable/ic_lock"
            app:chipIconSize="22dp"
            app:textStartPadding="2dp"
            app:textEndPadding="2dp"
            tools:text="1D"
            tools:visibility="visible" />
    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>